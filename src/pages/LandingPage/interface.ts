export interface FeatureProps {
  title: string;
  description: string;
  imageSrc: string;
  imageAtl: string;
  showPrimaryBgColor?: boolean;
}

export interface FeatureBoxProps {
  showPrimaryBgColor: boolean;
}
