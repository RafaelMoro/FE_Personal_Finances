export interface IAccountProps {
  title: string;
  amount: number;
  accountType: string;
  bgColor: string;
  color?: string;
}

export interface IAccountDynamicStylesProps {
  bgColor: string;
  color?: string;
}
